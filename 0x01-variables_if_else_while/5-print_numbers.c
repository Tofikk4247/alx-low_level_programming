#include <stdio.h>
/**
 * main - entry poit
 * Description: prints numbers 0 through9
 * Return: 0(Success)
 */
int main(void)
{
	int k;

	for (k = 0; k <= 9; k++)
	{
		printf("%d", k);
	}
	printf("\n");
	return (0);
}
